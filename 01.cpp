#include <stdio.h>
#include <string.h>

int main( ) {
   int num[] = {};
   int test[] = {};
   int count = 0;
   // scanf("%d",&count);
   scanf("%[^\n]",&test);
   printf("%d",test);
   // for (int i = 0; i < count; i++)
   // {
   //    scanf("%d",&num[i]);
   // }
   return 0;
}
